// update title
$('#steps-title').html('<%= j render "steps/title" %>');
// update steps
$('#step-list').html('<%= j render "steps/show" %>');
$(sortable('.sortable', true));
$('.editable').removeClass('hidden')
// remove new step form
$('.new-step-form').remove();
// show + button
$('.new-step a').show();


