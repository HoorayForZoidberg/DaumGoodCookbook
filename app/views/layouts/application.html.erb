<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Daum Good Cooking - by Jan, Julie, Sarah, Jill and Charlotte Daum</title>
    <meta name="description" content="This is the online version of the Daum family cookbook, containing the recipes of Jan, Julie, Sarah, Jill and Charlotte Daum.">
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
    <!-- ios support -->
    <link rel="apple-touch-icon" href="<%= image_path 'icons/apple-icon.png' %>">
    <meta name="apple-mobile-web-app-status-bar" content="#FFFFFF">
    <!-- SSL support for Google Domains -->
    <script>
      var host = "www.daumgoodcooking.com";
      if ((host == window.location.host) && (window.location.protocol != "https:")){
      window.location.protocol = "https";
      }
    </script>
  <link rel="manifest" href= "<%= asset_path '/manifest.json' %>" />
  <meta name="apple-mobile-web-app-capable" content="yes"></head>
  <body>
    <%= render 'shared/navbar' %>
    <%= render 'shared/flashes' %>
    <%= render 'shared/modal' %>
    <%= yield %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
