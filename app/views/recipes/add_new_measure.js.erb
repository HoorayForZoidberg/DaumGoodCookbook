swal("New measure added to the list!")
$('#modal-window').html('<%= j render "recipes/new_ingredient_modal" %>');
$('#new-measure-link').click(function(e) {
  e.preventDefault();
  $('#new-measure-form').removeClass("hidden")
});
